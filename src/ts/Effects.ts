//==============================================================================
//#region I/O

function getStoredTurtle() {}

function parseStoredTurtle() {}

function writeTurtleToStorage() {}

//#endregion
//==============================================================================

export function renameNode({ old, now }) {}

export function renameProperty({ old, now }) {}

function addClass({ label }) {}

function addProperty({ label }) {}

let generatorID = localStorage.generatorID || 0;
